spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/betclick_db
    username: betclick_runtime
    password: ${DB_RUNTIME_PASSWORD}
  flyway:
    url: jdbc:postgresql://localhost:5432/betclick_db
    user: ${DB_ADMIN_USERNAME}
    password: ${DB_ADMIN_PASSWORD}
jwt:
  secret: ${JWT_SECRET}
